# BUILD PLAN — dunchi-trader

**Plan ID:** PLAN-DUNCHI-TRADER-V1

| Field | Value |
|---|---|
| **Plan ID** | `PLAN-DUNCHI-TRADER-V1` |
| **Primary repo** | `dunchi-trader` |
| **Live repo** | `dunchitrader-collab/dunchitrader-collab.github.io` |
| **Created** | `2026-09-18` |
| **Estimate** | 70 |
| **Summary** | Build a village trades directory for Dunchideock as a plain static page that reads a published Google Sheet and needs no build step. The site must be operable and redeployable by anyone inheriting two account logins. |

---

## STATUS: SIGNED-OFF 2026-09-19

**Closed at 100% of 21 rows, TRIMMED from 24 on 2026-09-19; pre-trim position 83.7% (87/104 effort).**

That line is mandatory under **STD-00009 Rule 5.8.10.4 [CORE]**, and it is the most important line
in this document. **Without it a reader sees 100% and concludes everything was built.**

### What this 100% does and does not mean

**It reached 100% by MOVING 17 effort across three rows to a successor plan, not by building them.**
Rows **6.1**, **7.1** and **7.2** are `external` and owned by
**`PLAN-DUNCHI-LAUNCH-2026-09-19`**. They are excluded from this plan's denominator by their
status (Rule 5.4.2), which is why the figure reads 100% — the plan has finished everything it is
still accountable for, and the successor reports on the rest.

**Where this plan started:** `2026-09-18`, at 14 sub-tasks and an estimate of 70. It grew to 24 as
real gaps were found, and closes owning 21 of them.

### WHAT WAS NOT BUILT — stated plainly

**The inheritance requirement is UNPROVEN.** The Owner's own words in the Requirement as stated are
*"I want to be able to hand over the operating site to anyone else who inherits that google email
account"*, and **nobody has ever demonstrated that.** Row 6.1 asked for it and is leaving unbuilt.

**Worse, its runbook is KNOWN BROKEN**, measured 2026-09-19 and recorded in handover Layer 4:

- **Step 4** tells the walker to create a GitHub repository whose name is the **live site's**, so
  GitHub refuses it and **the walk stops dead at step 4**.
- **Steps 7 and 8** delegate to documents that open the **live spreadsheet** by its hard-coded id.
- **There is no cleanup section**, so a rehearsal leaves a **second public village directory** on
  the internet.

**The launch is not done either.** The village has not been told, and the list still carries test
people. Rows 7.1 and 7.2 cover that and are leaving unbuilt.

**Nothing was orphaned by the move** (Rule 5.8.10.1 question 2): no `done` row in this plan existed
only to serve 6.1, 7.1 or 7.2. The site, the feed, the search, the Call button, the recommendation
path and the verdict column are all delivered and all independently useful — which is why the
directory is working today even though it cannot yet be launched or handed on.

### What WAS delivered

A villager opens a link, taps a trade, and taps a large green Call button. Submissions publish
themselves. A villager can add a recommendation from the card and the village sees it. The page
holds up at the largest text size on a 320px phone and at 200% zoom. All of that is sighted by the
Owner on the live site, row by row.

---

## REQUIREMENT AS STATED

The following is the project owner's own wording, carried verbatim. It is not paraphrased at
any layer of this plan, and any sub-task that conflicts with it is wrong.

> "The key to this is really easy navigation. People will be using websites and they will be
> using their phone. Given it's the older generation, I expect that they will be zooming in
> quite a lot. So this needs to be a really simple, really quick and extremely easy to use
> web app."

> "Github pages is fine as long as the site easy VERY easy to search by work type."

> "The site needs to be dynamic. Most will use on their phone. However it must work on a
> computer too."

> "In all zoom functions make sure the text never rolls outside the box."

> "Nothing on this site must touch deverse. Everything must be registered to the dunchitrader
> email"

> "I want to be able to hand over the operating site to anyone else who inherits that google
> email account"

---

## HOW TO READ THIS PLAN

**A row is an OUTCOME, not a task.** The test each row must pass: could the owner look at the
result and say *"yes, that is delivered"*? "A villager can find a plumber and ring them from
their phone" is a row. "Set line-height to 1.55" is not — that is implementation detail, and
it lives in the solution design.

- **The design says HOW; this plan says WHAT IS DELIVERED.** The accessibility rules, the
  overflow rules, the two text-size bugs, the autocomplete behaviour, the verdict definitions
  and the CSV reading rules are all recorded in `docs/SOLUTION-DESIGN-dunchi-trader.md`. They
  are **cited here, never restated** — a second copy would drift from the first.
- **Detail belongs in a row's done-when**, which may list several checkable conditions, not in
  extra rows.
- **The owner's own actions are not rows.** Seeding the directory, posting the launch message
  and the seven Google-side operator actions are his, and are recorded in
  `docs/HANDOVER-dunchi-trader.md` Layer 4. Counting them here would inflate the plan with
  work the build does not do.
- Sub-tasks are numbered `{step}.{n}`. **Numbering is append-only from here on.**
- **Status** is one of `new`, `to-do`, `done`, `blocked`, `parked`, `descoped`, `external`.
- **Effort** is an integer 1–10, where 10 is the most work.

### Delivery fields — every row, no exceptions

Every row carries an **Owner**, a **Due start**, a **Due end**, an **Actual start** and a
**Forecast end**. A blocked row carries dates like any other: a blocked row still has a
forecast, and that is the point of having one.

- **Owner** is the account key `222b34c4-7d05-48f4-9d23-cfb47e96d9de` — Gavin. It is stored
  as the stable account identifier and the name is resolved at render, so a rename never
  orphans a row. He is accountable for every row; Claude Code is the tool, never the owner.
- **Actual start** is populated only for rows that have actually started. A row that has not
  begun carries a blank, which is information rather than an omission.
- The satellite tables carry the five delivery fields in their own columns — `Due start`,
  `Planned end`, `Actual start`, `Forecast end` — alongside `Owner`. `Pinned start` is left
  empty: no row here overrides the computed schedule.
- **Forecast end** is where the row is heading now, which may differ from its Due end.

**Scheduling is FORWARD from today, never backwards from a deadline.** The dates below start
at 2026-09-18, apply each row's effort and its dependencies, and let the end date fall out.
No target finish was chosen and worked back to.

**Estimate: 70 effort, recorded pre-decomposition.** The rows sum to 75, within 8% of it,
so the decomposition is consistent with the estimate rather than several times it.

**Rate: 60 minutes per effort point, over an 8-hour working day. This figure is ASSUMED, not
measured** — it was supplied as a working figure and can be adjusted once real throughput is
known. Nothing derived from it below is a measured duration, and the resulting dates are
projections, not commitments. The 8-hour day is this repo's first such convention; no earlier
one was recorded.

**Critical path: 49 of the 75 effort** — `1.1 → 2.1 → 3.1 → 4.1 → 4.2 → 6.1 → 7.1 → 7.2`.
Computed from the Depends-on column rather than assumed, and it matches the figure given when
the fields were commissioned. On the forward schedule the plan lands **2026-09-25**.

**One deliberate departure from the validator.** `STD-00009` §5.12 wants a *named person* in
`Owner` and reports the account key as "an opaque identifier, not a named person". The key is
used here because that is what was instructed, under decision `D12a-P3ud-09092026`: the estate
stores a stable account key and resolves the display name at render. The advisory is expected,
does not change the exit code, and is recorded here rather than silently accepted.

### Authority precedence

`design/wireframe-dunchi-trader.html` is a **working, reviewed page** — the layout, palette,
typeface, text-size control, autocomplete, search-clearing and overflow handling are already
built and were reviewed across eight iterations. Step 3 is therefore **port it, then verify
it**, not rebuild it feature by feature.

Where the wireframe and the design disagree, **the solution design §9 wins** and the wireframe
is corrected to match.

---

## Step 1 — Repo, remotes and hosting

The site exists at its public address and serves exactly what is committed.

**This step also satisfies the browser-reachability dependency for every later step.** The
owner tests rendered work in a live browser. A change is only built when it is **visible at
the URL he opens** — not when it is committed, and not when it works locally. Every later
step's done-when is checked against the live site, so nothing after this can be verified
until it is done.

| # | Status | Sub-task | Effort |
|---|---|---|---|
| 1.1 | done | The site is live at `https://dunchitrader-collab.github.io` and serves the committed source untouched. Done when a placeholder page carrying the site title and the small print is reachable at that URL on a phone and on a computer, the served HTML is byte-identical to the committed `index.html` (Jekyll processing disabled), and nothing resembling a credential or token is present anywhere in the repository. **2026-09-18T14:25:29Z — measured against the live URL: HTTP 200, `text/html`, 624 bytes; live sha256 `926e3141…babbeb8` equals committed `index.html` sha256 `926e3141…babbeb8`; `Jekyll v3.10.0` returns 0 matches, so `.nojekyll` is working; served `<title>` is `Dunchideock Village Suppliers`; the small print line is served. Credential scan clean. Computer half CONFIRMED. **CLOSED 2026-09-18T15:06:40Z — Owner sighting 2026-09-18T15:04Z: *"https://dunchitrader-collab.github.io all works are expected without any data in it though"*, given after opening the site on his phone and once on a computer. That covers the phone-and-computer clause, the only one outstanding; byte-identity, Jekyll-disabled and the credential scan were already measured. Re-verified this session: live sha256 equals committed `index.html`, 0 Jekyll matches, 0 credential matches.** | 5 |
| 1.2 | done | Both repositories carry the same source and either can be pushed to. Done when the `collab` remote and `origin` both push successfully, the two repositories hold identical content, and the push procedure is written down plainly enough for someone who has not seen this session to follow it. **CLOSED 2026-09-18T14:25:29Z — `origin` pushed (`5d48f55..f94d45a`) and `collab` pushed (`313ccfb..f94d45a`, `master:main`). Tree equality verified: both remotes resolve to tree `7b79e241`, and HEAD is 0 ahead of each. Push procedure written in `README.md`, including the `master:main` branch-name difference. The live repo's own unrelated `Initial commit` was merged rather than force-pushed over, so its history is preserved.** | 3 |

| # | Human | Machine | Outcome | Approach | State | Due | Forecast | Owner | Depends on | Pinned start | Due start | Planned end | Actual start | Forecast end |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1.1 | When Gavin opens the link on his phone to check the work, he sees the page. | Serve committed static source at the public URL | Later steps become verifiable, because rendered work can now be reviewed in a browser | Static files plus .nojekyll on Pages | done | 2026-09-18 | 2026-09-18 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | none |  | 2026-09-18 | 2026-09-18 | 2026-09-18 | 2026-09-18 |
| 1.2 | When Gavin has finished a change here and wants the village to see it, he pushes to the live site. | Configure both remotes and document the push | The inheritable repo stays a full copy rather than drifting behind | Second named git remote | done | 2026-09-18 | 2026-09-18 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 1.1 |  | 2026-09-18 | 2026-09-18 | 2026-09-18 | 2026-09-18 |
---

## Step 2 — Live data

The live page shows the real tradespeople from the sheet, grouped by trade.

**Precondition, measured 2026-09-18 and still failing.** The published CSV URL serves the
**raw Form responses tab**, not the curated Published tab. Built against it today, the site
would publish unreviewed submissions and bypass the approval gate entirely. Sub-task 2.1 is
therefore `blocked` on the owner republishing from the correct tab.

| # | Status | Sub-task | Effort |
|---|---|---|---|
| 2.1 | done | The page shows the real tradespeople from the Published tab, grouped by trade. Blocked on the owner republishing the CSV from the Published tab. Done when the live page lists every active row from the sheet under the right trade heading, a person holding two trades appears under both without being counted twice, inactive rows do not appear, trades with nobody in them are absent entirely, and each card shows the name, business, trade and a working Call button. Reading rules per solution design §6.1.1 — columns matched by header name, quoted fields parsed properly. **2026-09-18T14:39:30Z — the machinery is BUILT, LIVE and TESTED at `d10ae18`; only the feed is missing. Proven against a correct Published-tab fixture: 3 active people yield 4 listings, a two-trade person appears under both headings while counted once, the inactive row is excluded, and no empty trade appears. Header-name mapping verified by re-ordering every column. STILL BLOCKED — the live feed serves the raw Form responses tab, so there is nothing correct to render. This row closes when Gavin republishes from the Published tab; no further code is expected.** **2026-09-18T15:20Z — FEED SWITCHED AND MEASURED; awaiting the owner's sighting. `app.js` now reads gid `1915382769` (commit `0267ef3`), verified live cache-busted. Feed header exactly `id,first_name,last_name,business,phone,trade,extra_trade,status`, 4 data rows. Driven in a real browser against the LIVE site: 4 trade headings render (Electrician, Heating, Plumber, Roofer); each active row appears under its heading with business, trades and a working `tel:` Call button; `T002` appears under BOTH `Electrician` and its `extra_trade` `Heating` from one row and is counted once; the `hidden` row produces no heading and is not findable by search. Desktop 1280px renders the same four with 0px overflow. OUTSTANDING — a rendered surface: the owner opens the site with this data on his phone and confirms it reads correctly.** **2026-09-18T17:04:41Z — CLOSED. The rendered surface exists. Owner's sighting 2026-09-18 (~16:50Z), verbatim: *"Task 1 is a pass."* He was asked to open `https://dunchitrader-collab.github.io` on his phone, tap Plumber, press the largest A, and confirm a card shows name, business and a working green Call button, that it reads correctly, and that the phone number stays in one piece with nothing running off the right edge. That supplies the clause every prior measurement could not: a human confirming it READS correctly on a real phone, including name and business on the card. The structural clauses were ALSO independently re-measured in this session against the LIVE site in a real browser at 320px (HeadlessChrome 148, 2026-09-18T17:03Z): 4 trade headings render — Electrician, Heating, Plumber, Roofer — each showing a count of 1; `Test Sparky` appears under BOTH `Electrician` and `Heating` from a single row and is counted ONCE under each, never twice; the hidden/inactive row produces no heading and no card; no empty trade appears; and every card carries a working `tel:` anchor (`tel:07700900456`, `tel:07825736940`, `tel:01392123456`, all `<a>` elements). Every done-when clause is therefore satisfied by measurement in this session, with the owner's sighting supplying the rendered-surface and name/business confirmation. Note for the record, not a defect: on the Plumber and Roofer cards the business line is not visually distinct from the name, which is consistent with those rows carrying a business equal to the name in the feed; the owner confirmed name AND business on the Plumber card, so the clause is met on his sighting.** | 8 |
| 2.2 | done | The page stays useful when the sheet is empty or the feed is unreachable. Done when a sheet with no data rows, a blocked or timed-out fetch, and a feed whose header is not the expected schema each produce a readable message rather than a blank page or a silent console error. Per solution design §6.1.1 rule 3 and §7.4. **CLOSED 2026-09-18T14:39:30Z. All three states render prose in the page's own voice, not an error string — "Nobody on the list just yet", "The list will not load at the moment", "The list is being updated". The wrong-header state was demonstrated against the REAL live condition rather than a mock: the live `app.js` run against the live feed parses 3 rows, `build()` returns `null`, and the page shows the message and nothing else. Safety property proven with a fixture carrying 2 email addresses — 0 reached the rendered DOM, and no raw form question text leaked. 14 logic tests pass, including quoted commas, embedded newlines, doubled quotes, and re-ordered columns.** | 4 |

| # | Human | Machine | Outcome | Approach | State | Due | Forecast | Owner | Depends on | Pinned start | Due start | Planned end | Actual start | Forecast end |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 2.1 | When a villager needs a plumber and taps Plumber, they expect to see the plumbers the village actually recommends. | Fetch and render the Published tab by trade | The directory stops being a placeholder and becomes the real list | Client-side CSV fetch, parse and group | done | 2026-09-21 | 2026-09-18 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 1.1 |  | 2026-09-21 | 2026-09-18 | 2026-09-18 | 2026-09-18 |
| 2.2 | When a villager opens the link while Gavin is mid-edit on the sheet, they never meet a blank screen. | Render explicit empty and failure states | A bad feed reads as a message, never as a broken site | Explicit branches before render | done | 2026-09-18 | 2026-09-18 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 2.1 |  | 2026-09-18 | 2026-09-18 | 2026-09-18 | 2026-09-18 |
---

## Step 3 — Front end

The agreed design is live, and it holds up at the largest text size on a narrow phone.

The wireframe is the implementation (see Authority precedence above). This step ports it and
then verifies it against the binding rules in solution design §9.

| # | Status | Sub-task | Effort |
|---|---|---|---|
| 3.1 | done | The live site looks and behaves like the agreed wireframe on a phone and on a computer. Done when `design/wireframe-dunchi-trader.html` is ported to the live page, the text-size control and its remembered setting work, the small print and the village chat line are both visible, and no user-facing text says "tradesman". **2026-09-18T14:39:30Z — ported and live at `d10ae18`. `index.html` 1,845B, `style.css` 10,988B, `app.js` 14,060B, all three HTTP 200 and sha256-identical to the committed files. Verified in the served output: small print and village chat line both present; "tradesman" 0 matches; demo scaffolding 0 matches; header not sticky (0 `position:sticky`); sizer strip entirely in px (0 `rem` in that block); `minmax(min(100%, 15rem), 1fr)` present; two-line Call button present; autocomplete `mark` uses `color:inherit` + underline only. The only CSS rule dropped from the wireframe is `.demo`; the only ones added are `.notice` and `.loading`. **CLOSED 2026-09-18T15:06:40Z — Owner sighting 2026-09-18T15:04Z: *"https://dunchitrader-collab.github.io all works are expected without any data in it though"*, given after opening the site on a phone and a computer and pressing the largest A. Every clause of this row is visible without data on the page: the text-size control and its remembered setting, the small print, the village chat line, and the wording. The sighting covers them all.** | 6 |
| 3.2 | done | A villager can find someone by work type without knowing a name. Done when tapping a trade shows that trade's people, typing in the search box suggests trades ahead of people and businesses, the trade list stays visible while suggestions show, and tapping a trade or Back clears the search box. Behaviour per solution design §9.11. **CLOSED 2026-09-18T15:02Z — 26 behaviour tests driven in a real browser (Chromium/puppeteer), all passing: tapping a trade shows that trade's people and clears the search; Back clears it too and returns to the 8-trade grid; typing suggests TRADES ahead of people and businesses (verified by index position); matches are UNDERLINED with `color` equal to the parent's, so colour carries no meaning alone; the trade list stays visible while suggestions show; Enter searches everything and Escape clears. NOT RUN AGAINST REAL DATA — the live feed still serves the wrong tab, so every search test used a correct Published-tab fixture.** | 6 |
| 3.3 | done | The page survives the way this audience actually uses it. Done when, at the largest text size on a 320px-wide phone and again at 200% browser zoom, nothing scrolls sideways, no text leaves its box, no phone number breaks mid-digit, the header does not swallow the screen and the size buttons do not grow as they are pressed. Rules and the two fixed bugs per solution design §9.6–§9.10. **2026-09-18T15:02Z — measured in a real browser at 320px across six cases: default 20px, largest 29px, 200% zoom, largest+cards, largest+cards+200% zoom, and desktop 1280px. `scrollWidth` equals `clientWidth` in ALL SIX (0px sideways overflow), 0 elements past the right edge, header `position: static` in every case, size buttons a constant 48×44px. Three real defects were found and fixed: a long international number split mid-digit (3 lines at largest size, 13 at 200% zoom); the fixed-px size strip needed 108px of a 102px line under zoom; and rem-scaled padding on `.wrap`/`.card`/`.call` claimed 174px of a 160px body, collapsing the content box. Every digit group now stays intact at every size measured. **2026-09-18T15:06:40Z — PARTLY covered by the owner's sighting (Owner sighting 2026-09-18T15:04Z: *"https://dunchitrader-collab.github.io all works are expected without any data in it though"*), given after pressing the largest A on his phone. That covers: nothing scrolls sideways, no text leaves its box, the header does not swallow the screen, and the size buttons do not grow. STILL OPEN ON ONE CLAUSE — *"no phone number breaks mid-digit"* CANNOT have been shown by that sighting, because the feed still serves the wrong tab, so no cards and therefore NO PHONE NUMBER were on screen. The row is deliberately held open on that single clause rather than closed on evidence that could not have shown it. It closes when 2.1 is unblocked and the owner presses the largest A with real cards visible.** **2026-09-18T15:20Z — THE OUTSTANDING CLAUSE IS NOW MEASURED WITH REAL CARDS. At the largest text size (root 29px) on a 320px phone, with real phone numbers on screen for all three active rows: `07825 736940` groups [07825:1 line, 736940:1 line]; `07700 900456` groups [07700:1 line, 900456:1 line]; `01392 123456` groups [01392:1 line, 123456:1 line] — NO group split mid-digit, and document overflow 0px in every case. That is the clause the owner's 15:04Z sighting could not cover, because no cards were on screen then. OUTSTANDING — still a rendered surface: the owner presses the largest A with this data visible and confirms nothing runs off the edge.** **2026-09-18T17:04:41Z — CLOSED. Owner's sighting 2026-09-18 (~16:50Z), verbatim: *"Task 1 is a pass."* The task he was passing was precisely this row's outstanding clause: open the live site ON HIS PHONE, tap Plumber, PRESS THE LARGEST A, and confirm a card shows name, business and a working green Call button, that it READS CORRECTLY, and that THE PHONE NUMBER STAYS IN ONE PIECE WITH NOTHING RUNNING OFF THE RIGHT EDGE. Every clause of this row's done-when is covered, and — unlike the 15:04Z sighting which had to be rejected for this clause — real cards with real phone numbers WERE on screen this time, because the feed was switched at `0267ef3`. Mapping clause by clause: *"at the largest text size on a 320px-wide phone"* — he pressed the largest A on his own phone; *"nothing scrolls sideways"* and *"no text leaves its box"* — he confirmed nothing runs off the right edge; *"no phone number breaks mid-digit"* — he confirmed the phone number stays in one piece, with a real number visible, which is the exact clause held open at 15:06:40Z and measured but not sighted at 15:20Z; *"the header does not swallow the screen"* and *"the size buttons do not grow as they are pressed"* — covered by the earlier 15:04Z sighting (header `position: static`, buttons constant 48×44px across six measured cases) and unchanged by the feed switch, and he reported the page reads correctly after pressing the largest A. No clause is closed on evidence that could not have shown it.** | 5 |
| 3.4 | done | One mistyped capital cannot split a trade into two tiles. Done when the live page groups trades ignoring capitals and shows the first spelling it met, so rows carrying `Plumber` and `plumber` land under a single heading with the right count, and the tiles already on the site still render unchanged at 320px. Proven on the SERVED page in a real browser. **2026-09-18T19:31Z — CLOSED ON SERVED-PAGE EVIDENCE at commit `b8b54be`. Owner's ruling D6a. Grouping now keys on the lower-cased trade while displaying the FIRST spelling encountered; all three places that keyed on the raw trade were moved to the shared key — the tile grid, the autocomplete counts, and the filter applied when a trade is tapped — so none of them can disagree. DRIVEN IN A REAL BROWSER AGAINST THE LIVE SITE at 320px with a feed fixture substituted at the network layer, so the owner's sheet was never touched: rows carrying `Plumber` and `plumber` render **ONE tile reading `Plumber` with a count of 2**, and tapping it shows a heading of `Plumber` and **2 cards**. With the lowercase spelling appearing FIRST the single tile reads **`plumber`**, which is what proves the display name is the first spelling met rather than a capitalised or lower-cased rewrite. Four spellings together — `Plumber`, `plumber`, `PLUMBER` and `" Plumber "` — collapse to **one tile, count 4, 4 cards**. REGRESSION CHECKED against the four live trades: `Electrician`, `Heating`, `Plumber`, `Roofer` still render as four separate tiles with a count of 1 each, the hidden row is still absent, `Heating` still appears via `extra_trade`, and **document overflow is 0px in every case**.** | 2 |
| 3.5 | done | A villager reading a listing sees why a neighbour recommended that person, and who to ask about it. Done when a card carrying recommendations shows each villager's own words and the name of the villager who gave them — or "a villager" where the name was left blank — read from the live feed rather than from the visitor's own browser, with a card carrying none showing no empty heading and a popular card staying readable rather than becoming a wall of text. Proven on the SERVED page in a real browser at 320px and at the largest text size, with nothing overflowing sideways and the Call button and vote panel still behaving. **2026-09-18T19:44Z — CLOSED ON SERVED-PAGE EVIDENCE at commit `91a3f4c`.** The words and the villager's name now travel Form → Published K/L → feed → card. `app.js` reads `recommendations` and `recommended_by` as OPTIONAL columns, so a sheet predating them still renders rather than failing. **MEASURED IN A REAL BROWSER AGAINST THE LIVE SITE at 320px, at BOTH the default size (root 20px) AND the largest (root 29px), with a feed fixture at the network layer so the owner's sheet was untouched.** A card with THREE: tally reads *"3 villagers recommend this tradesperson"*, the two most recent show in full attributed to `Mary` and `Tom`, and the rest sit behind one full-width button reading *"Read 1 more recommendation"*; tapping it reveals all three and removes the button. A card with ONE: shown in full, attributed to `Sue`, no button. A card with NONE: no tally and no empty heading — nothing appears at all. A recommendation containing a comma AND an escaped quotation mark (*said "no charge"*) survived the CSV round trip intact. The Call button still carries the right `tel:` on every card and the vote panel is still present. **Overflow 0px and 0 elements past the right edge in all six cases.** The two-then-tap design is deliberate: at 29px on a 320px screen a single recommendation already fills much of the screen, so a popular tradesperson's card would otherwise bury the Call button under a wall of text. **STATED PLAINLY AND NOT GLOSSED: a recommendation typed into the site's own panel still appears only to the visitor who typed it.** It posts to the Votes tab, and nothing copies Votes into Published, so other visitors do not see it until the owner moves it across by hand. Only recommendations arriving through the GOOGLE FORM reach everybody, via `Publish.gs`. That gap is real, is not closed by this row, and is recorded as an outstanding item in the handover.** | 4 |

| # | Human | Machine | Outcome | Approach | State | Due | Forecast | Owner | Depends on | Pinned start | Due start | Planned end | Actual start | Forecast end |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 3.1 | When a 78-year-old villager opens the link for the first time, the page looks calm and readable. | Port the reviewed wireframe to the live page | The eight iterations of agreed design reach the real site | CSS and markup port from wireframe | done | 2026-09-18 | 2026-09-18 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 2.1 |  | 2026-09-18 | 2026-09-18 | 2026-09-18 | 2026-09-18 |
| 3.2 | When a villager's gutter is overflowing and they know no roofers by name, they search by the work. | Wire trade filtering and search suggestions | Searching by work type is the primary route, as the owner required | In-memory match over loaded rows | done | 2026-09-18 | 2026-09-18 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 3.1 |  | 2026-09-18 | 2026-09-18 | 2026-09-18 | 2026-09-18 |
| 3.3 | When a villager with poor eyesight presses the biggest A, the phone number stays readable. | Verify layout at largest size and 200% zoom | The zoom requirement is proven on a real narrow screen | Narrow-viewport and zoom testing | done | 2026-09-18 | 2026-09-18 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 3.1 |  | 2026-09-18 | 2026-09-18 | 2026-09-18 | 2026-09-18 |
| 3.4 | When a neighbour looks for a plumber, everyone recommended as one is under the same heading. | Group trade tiles ignoring capitals | A typing slip cannot hide half a trade from the village | Case-insensitive grouping key, first spelling displayed | done | 2026-09-22 | 2026-09-18 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 2.1 |  | 2026-09-18 | 2026-09-22 | 2026-09-18 | 2026-09-18 |
| 3.5 | When a villager reads a listing, the neighbour's own words are what tell them to ring that person. | Render recommendations and their author on each card | The directory becomes a recommendations list rather than a phone list | Read the words from the feed and show them on the card | done | 2026-09-23 | 2026-09-18 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 2.1 |  | 2026-09-18 | 2026-09-23 | 2026-09-18 | 2026-09-18 |
---

## Step 4 — Recommendations from the site

A villager can add their own recommendation from the card, and it reaches the sheet.

> **The step's name changed on 2026-09-19 and the reason is recorded rather than
> silently applied.** It was **Step 4 — Votes**, after the tab its endpoint wrote
> to. Under the owner's ruling **D1b-6G7f-19092026** that endpoint now writes to
> the **Published** tab, and nothing reads or writes Votes any more, so the old
> name pointed at a dead thing. **No row number changed and no row's text was
> edited** — renaming the heading does not renumber anything, and row 4.6's
> wording still says "Votes tab" deliberately, with its moved destination
> recorded in handover Layer 5 under D1b rather than by rewriting the row.

| # | Status | Sub-task | Effort |
|---|---|---|---|
| 4.1 | done | A villager can add a recommendation to someone already listed, without leaving the page or re-typing anything. Done when the in-place panel opens on the card headed "You are recommending {name}", a blank name records as "a villager", a too-short entry shows a plain-text error without clearing what they typed, and the tally and their words appear on the card. Behaviour per solution design §7.4 and §7.5. **CLOSED 2026-09-18T15:02Z — driven in a real browser: the panel opens in place on the card headed "You are recommending Dave Trelawny" without leaving the page; a blank name records as "a villager"; a too-short entry shows a plain-text error positioned BELOW the textarea whose colour equals the body colour (so colour alone carries nothing) and does NOT clear what was typed; the tally and the villager's words then appear on the card. THE ENDPOINT DOES NOT EXIST YET — the send sits behind `sendRecommendation()`, which returns `false` and posts nothing while `VOTES_ENDPOINT` is empty. Row 4.2 sets that constant. The confirmation deliberately says the recommendation is "noted on this page" and adds that it will not reach the village list until the site is finished, so the thank-you does not lie.** | 7 |
| 4.2 | done | Those recommendations reach the sheet through an endpoint that can do nothing else. Done when a vote placed on a real phone appends one row to the Votes tab carrying the trader id rather than the name, an attempted write to the Published tab fails, and the Apps Script source is committed to the repository so it can be redeployed from scratch. **2026-09-18T15:06:40Z — BUILT except the owner's deployment, which only he can do. [PATH] `apps-script/Code.gs` committed: it can ONLY `appendRow` to a tab named `Votes`, the tab name is a hard-coded constant with no parameter able to change it, there is no read call anywhere in the file, and no edit or delete path. 21 logic tests pass against a `SpreadsheetApp` stub, including four adversarial attempts to reach `Published` — every write went to Votes, the script never read any tab, and Published and the responses tab were byte-unchanged. [PATH] `apps-script/DEPLOY.md` carries the numbered paste-ready deployment steps and the Votes header row. NOT RUN AGAINST A REAL SHEET. `VOTES_ENDPOINT` stays `""` — no URL invented. Closes only when a vote from a real phone lands in the Votes tab.** **2026-09-18T17:04:41Z — ENDPOINT NOW WIRED AND SERVED; ROW DELIBERATELY STAYS OPEN. The owner deployed the Apps Script under `dunchitrader@gmail.com` per `apps-script/DEPLOY.md` steps 1–3 and supplied the Web app URL, which is set in `app.js` at commit `4d67c15` — set in this repository and pushed to BOTH remotes rather than hand-edited in the live repo per DEPLOY.md step 4, so the two repositories stay identical. MEASURED SERVED, not merely committed: `app.js` fetched cache-busted from `https://dunchitrader-collab.github.io` carries the exact endpoint on line 30, and the served sha256 `4c628ce0813e3a4caf4ae6ebbbbb7ad08e162a19cb849cb3071c565733e77c13` is byte-identical to the committed file. REPORTED BY THE OWNER, NOT MEASURED HERE: that the Votes tab exists with the four-column header, and that the web app is deployed executing as himself with access set to Anyone. THIS ROW IS NOT CLOSED. Its own wording closes it only when a vote placed ON A REAL PHONE appends a row to the Votes tab. Row 4.3's server-driven browser vote is not a phone and does not close it, and the opaque cross-origin response means even that vote cannot confirm the append from the browser side. Closes when the owner places a vote from his phone and sees the row in the Votes tab.** **2026-09-18T17:48Z — CLOSED. The owner placed a vote from his phone on the live site and confirmed the row reached the Votes tab. **Owner confirmation 2026-09-18 (~17:47Z), verbatim: *"the vote populated"*.** That is the clause this row was held open on across three sessions, and it is the only clause a build could not supply for itself. Checked clause by clause against this row's own wording, because "the vote populated" is one sentence and the row has three requirements: **(1) "a vote placed on a real phone appends one row to the Votes tab carrying the trader id rather than the name"** — the append is the owner's confirmation above, on a real phone, on the live site; the *id-not-name* half is satisfied by construction in the shipped code and was verified by reading it this session: `app.js` calls `sendRecommendation(l.id, by, why)`, so the tradesperson is identified ONLY by the stable id, and the `name` field carries the VILLAGER's own name (or the literal `"a villager"` when left blank), never the tradesperson's; `apps-script/Code.gs` line 146 appends `[new Date(), id, name, text]`, putting the trader id in column B. **(2) "an attempted write to the Published tab fails"** — satisfied by a stronger property than a runtime failure, and this is deliberate: there is NO code path that can address `Published` at all. The target tab is the hard-coded constant `VOTES_TAB = 'Votes'` (line 39), never derived from anything the caller sends, with a `FORBIDDEN_TABS` guard checked before any sheet is opened (lines 137–139); the file contains no read call, no edit path and no delete path. An attempted write to Published is therefore not expressible through this endpoint rather than merely rejected by it. REPORTED, not measured this session: the 21 stub logic tests from the 2026-09-18T15:06:40Z session, including four adversarial attempts to reach Published, every one of which went to Votes with Published byte-unchanged. **(3) "the Apps Script source is committed to the repository so it can be redeployed from scratch"** — measured this session: `apps-script/Code.gs` (5,733 bytes) is committed at `3bdf845` and present in BOTH remotes (`origin/master` and `collab/main`), alongside `apps-script/DEPLOY.md` carrying five numbered deployment steps. **LIMIT STATED PLAINLY:** this session did not and cannot verify that the script currently DEPLOYED in the owner's Apps Script project is byte-identical to the committed `Code.gs` — he pasted it, and only he can see it. Clause 3 asks that the source be committed so it CAN be redeployed from scratch, which is measured and true; it does not assert that what is running today came from that file. See also Layer 3 on the first attempt that did not reach the sheet.** | 6 |
| 4.3 | done | The cross-origin behaviour is known rather than assumed. Done when the actual response behaviour from the live Pages origin is measured and written down, and solution design §6.2 is corrected to record what was observed rather than what was expected. **2026-09-18T17:02:32Z — MEASURED AND WRITTEN DOWN. Unblocked by 4.2's endpoint being wired at `4d67c15` and served live. A real Chromium browser (HeadlessChrome 148, 320px mobile viewport) was pointed at the LIVE site `https://dunchitrader-collab.github.io`, Plumber was tapped, and ONE recommendation was submitted through the card's own panel — the shipped path, not a synthetic fetch. `window.fetch` was wrapped only to observe what the shipped call returned; the call itself was unaltered. WHAT THE PAGE CAN SEE: the promise RESOLVES (it does not throw or reject), `Response.type` `opaque`, `status` `0`, `ok` `false`, `redirected` `false`, `url` `""`, ZERO readable headers, round trip **2779.9 ms**. WHAT THE NETWORK LAYER RECORDS, invisible to the page: `POST` to `…/exec` with `referer: https://dunchitrader-collab.github.io/` returns **HTTP 302** carrying `access-control-allow-origin: *` and a `location` to `script.googleusercontent.com/macros/echo`; the browser DOES follow that redirect as a `GET`, which returns **HTTP 200**; the final response is then `net::ERR_ABORTED` because no-cors discards the body, which is the normal end of a successful opaque post and not an error. TWO CORRECTIONS TO THE OLD ASSUMPTION, both now in §6.2: the promise resolves rather than rejecting, so a `catch` will never fire on a failed write and `ok`/`status` carry no information and must not be branched on; and Apps Script DOES return `access-control-allow-origin: *` — the reply is unreadable because `mode: 'no-cors'` discards it, not because the header is absent. The design's optimistic-thank-you conclusion STANDS and is now measured rather than inferred. Solution design §6.2 rewritten to record the observation with the superseded inference preserved, and risk-register row 7 closed. LIMIT OF THIS MEASUREMENT, STATED PLAINLY: the opaque response proves the request was sent and the script answered 302→200; it does NOT prove `appendRow` ran or that a row reached the Votes tab — that is row 4.2 and is the owner's to confirm. A test row was deliberately written and is labelled for deletion; see the handover Layer 1 and Layer 6.** | 2 |
| 4.4 | done | The shipped code and the inheritance documents stop contradicting the deployed reality. Done when nothing a maintainer or the owner reads still says the votes endpoint does not exist: the comments in `app.js` describe the deployed endpoint and the measured opaque-response behaviour citing solution design §6.2, `apps-script/DEPLOY.md` step 4 tells the owner the value is already set in source and pushed to both remotes rather than telling him to hand-edit the live repository, and the live site no longer returns 404 for `/favicon.ico`. Closes on measurement against the SERVED site, not against the commit. **2026-09-18T17:25Z — CLOSED ON SERVED-SITE MEASUREMENT at commit `99b66de`. (1) `app.js` comments: the three blocks claiming the endpoint does not exist are replaced. They now describe the deployed bounded endpoint, state that the return value is NOT a success signal, and carry the §6.2 measurement — opaque reply, `status` 0, zero headers, and the trap that the promise RESOLVES rather than rejecting, so `catch`/`ok`/`status` must never be branched on. **Proven comment-only:** with comments and blank lines stripped, the code is byte-identical to `93c2777`, and `node --check` passes. (2) `apps-script/DEPLOY.md` step 4 no longer tells the owner to hand-edit `app.js` in the live repository; it records that the value went in at `4d67c15`, explains in plain English that editing one repository of two makes them disagree and that the next push would silently revert it, and tells him what to do if a redeploy ever yields a new `/exec` URL. The opening and step 5 were corrected in the same pass — step 5 now carries the measured fact that the page thanks the villager either way, so the Votes tab is the only proof. (3) The favicon 404 is gone. MEASURED IN A REAL BROWSER ON THE LIVE SITE, plain page load, no vote placed: **BEFORE — 1 failed request (`/favicon.ico` → 404) and 1 console error; AFTER — 0 failed requests and 0 console errors.** The icon is an inline SVG data URI (527 bytes) in the Call button green `#0A4F2E`, so there is no build step, no binary in the repo and no extra request; it was verified to actually DECODE and paint (`naturalWidth` 150) rather than merely being declared. Served `index.html` and `app.js` are byte-identical to the committed files (sha256 `66ba1299…070d2b43` and `1276f60f…a7e1f92c`).** | 2 |
| 4.5 | done | A villager describing a small job in a few words is not turned away. Done when the recommendation panel accepts a note of seven characters, still refuses one of six, and the message it shows when it refuses says truthfully what it is asking for — proven on the SERVED page in a real browser, with the message's colour still equal to the body colour so colour alone carries no meaning. **2026-09-18T18:30Z — CLOSED ON SERVED-PAGE EVIDENCE at commit `81385b7`. Owner's ruling 2026-09-18 (~18:26Z), verbatim: *"I think we can change the check to 7 characters. As they can't really put in a sentence without more than that "Fixed Gate" is about as short as you can get."* `app.js` `MIN_WORDS` 15 → 7, and the refusal message rewritten because the old one had stopped being true: *"Please write a few more words"* asks for "a few more" when seven characters is about two words, and it never said what would be enough. It now reads **"Please add a word or two more — even "Fixed gate" is enough."**, which names a length the villager can picture and uses the owner's own example. Exactly two lines of behaviour changed; nothing else in the panel was touched, and `node --check` passes. DRIVEN IN A REAL BROWSER AGAINST THE LIVE SITE at 320px, with the votes POST blocked so no row was written to the owner's sheet: **six characters ("Fix it") is REFUSED** — the panel stays open, the message is visible reading exactly the new text, what was typed is preserved, and `aria-invalid="true"` is set; **seven characters ("Fixed g") is ACCEPTED** — the panel closes; and the owner's own **"Fixed gate" (10) is accepted and renders on the card** as `"Fixed gate"` under `1 villager recommends this tradesperson`. **The message colour is `rgb(16, 20, 19)` and the body colour is `rgb(16, 20, 19)` — equal, so colour alone carries no meaning**, which is the §9 accessibility rule row 4.1 was also held to. The sheet's `CHECK THIS` trigger moved to `<7` in the same commit so the page and the sheet agree, and solution design §7.4 and §7.2 were corrected with the ruling quoted.** | 2 |
| 4.6 | done | A short recommendation the page accepted is not silently thrown away by the endpoint. Done when the votes endpoint's minimum length matches the seven characters the page and the sheet already use, so a villager writing "Fixed gate" has their words recorded rather than dropped without anyone being told. Closes when the owner has re-pasted the script and redeployed it as a new version of the existing deployment, and a short recommendation reaches the Votes tab. **2026-09-19T13:35Z — CLOSED ON THE OWNER'S SIGHTING OF THE WHOLE CHAIN, ON THE LIVE SITE.** He opened `https://dunchitrader-collab.github.io/` with a cache-busting parameter, tapped a trade, tapped **I recommend them too** on **`Frank Shelly`** (Carpenter, `07111655432`), typed a name and a note, and submitted. He then confirmed the recommendation had reached the **Published** tab — **columns K and L carry his words and his name** — and reloaded the live page with a **FRESH** cache-busting parameter, so the card was rendered from the published feed rather than from any in-page state, and **saw his words and his name on Frank Shelly's card**. That second cache-bust is what makes this a sighting of the PATH rather than of the browser's own memory: the page that showed his words had never seen his submission. **The chain is proven end to end — villager taps, the endpoint writes Published, the village sees it, and nobody touched a spreadsheet.** **This row asked that a short recommendation the page accepted is not silently dropped by the endpoint.** The seven-character minimum went in at `f8e34ff`; the deployed copy carried it from the owner's 2026-09-19 redeploy (DEPLOY.md step 14 Part A), and his note above passed through it and landed. Closed on the same sighting as 4.7 because one submission exercised both. | 1 |
| 4.7 | done | A recommendation placed on the site reaches the village list by itself. Done when a vote placed from the live site on a real phone appends the villager's words and name to that person's card on the live page without the Owner touching the spreadsheet. **2026-09-19T11:25:10Z — BUILT AND TESTED, AWAITING THE OWNER'S SIGHTING; ROW STAYS `new` BECAUSE STD-00009 HAS NO STATUS FOR BUILT-AWAITING-VERIFICATION** (§5.2.1 [CORE]: the seven tokens are `done`, `to-do`, `blocked`, `new`, `parked`, `descoped`, `external` — none means "done pending confirmation"). Owner's ruling **D1b-6G7f-19092026**: the on-site panel feeds the **Published** tab, not Votes, because nothing reads Votes and a villager's words landed where no neighbour would see them. `apps-script/Code.gs` at `f8e34ff` appends the words to Published `K` and the name to `L` of that trader's row, through **`appendRecommendation()` in `Publish.gs` — the same function the row-5.2 publisher calls for a duplicate form submission**, not a second copy (asserted by test 18, which substitutes a sentinel for the shared function). Bounded per **D2-6G7f-19092026** and enforced in code: it cannot create a row, assign or reuse an id, write any column but `K` and `L`, touch the owner's formula columns `I` and `J`, or reach an unknown or `hidden` id — those are refused and logged with nothing written. It reads via `tableValues()`, never `getLastRow`/`getDataRange`, because the owner's whole-column `ARRAYFORMULA` helpers make the sheet look 1000 rows tall; the test's negative control proves a naive implementation would write to row 1001 instead of row 5. **66 endpoint tests pass** against a stub whose `I` and `J` hold formula objects so a write to them is detectable, including nine adversarial posts each attempting to steer the write elsewhere — every one landed in `K`/`L` of one row, Votes was never opened, and both formulas survived. **30 checks pass driven in Chrome 148** against the real page with the feed stubbed and the POST blocked, so the owner's sheet was never touched: the body sent is `{id, name, text}`, it carries the trader id and not the name, and it goes to the recorded `/exec` address. **WHAT IS NOT PROVEN, AND IT IS THE WHOLE OF THIS ROW:** nothing here has run against the real Google Sheet, because the deployment does not yet carry this script — see the measured `[BUG]` in handover Layer 3. This row closes on the owner's sighting of his own words appearing on a real card on the live page, and on nothing less. **2026-09-19T13:35Z — CLOSED ON THE OWNER'S SIGHTING OF THE WHOLE CHAIN, ON THE LIVE SITE.** He opened `https://dunchitrader-collab.github.io/` with a cache-busting parameter, tapped a trade, tapped **I recommend them too** on **`Frank Shelly`** (Carpenter, `07111655432`), typed a name and a note, and submitted. He then confirmed the recommendation had reached the **Published** tab — **columns K and L carry his words and his name** — and reloaded the live page with a **FRESH** cache-busting parameter, so the card was rendered from the published feed rather than from any in-page state, and **saw his words and his name on Frank Shelly's card**. That second cache-bust is what makes this a sighting of the PATH rather than of the browser's own memory: the page that showed his words had never seen his submission. **The chain is proven end to end — villager taps, the endpoint writes Published, the village sees it, and nobody touched a spreadsheet.** **Checked against this row's own done-when, clause by clause:** *a vote placed from the live site on a real phone* — yes, the live URL, cache-busted; *appends the villager's words and name to that person's card* — yes, Published `K` and `L`, then visible on the card; *on the live page* — yes, re-fetched from the published feed; *without the Owner touching the spreadsheet* — yes, he only read it to confirm. Every clause is covered by what he did. | 4 |
| 4.8 | done | A villager is never shown words on a card that have not reached the village list, and a card stays readable when three people have recommended the same tradesperson. Done when a recommendation submitted from the panel does NOT appear on the card until it has come back through the published feed, the thank-you message and the five-minute notice both remain, and a card carrying three recommendations is measured at the largest text size on a 320px phone and again at 200% zoom with nothing overflowing its box. **Owner's ruling D5b-6G7f-19092026.** Measured 2026-09-19: he submitted a recommendation and the card showed his words and his name while the Published tab still held nothing. `app.js` keeps recommendations added during a visit in an in-page `VOTES` object and draws them immediately; because the endpoint's reply is opaque by design the page cannot know the write succeeded, **so on a genuine failure a villager sees their own words on the card, attributed to them, and believes permanently that their recommendation reached the village list.** A message is a claim; a card looks like data. The screen also contradicts itself — the notice says it takes five minutes to appear while the page has already shown it. **Closes on the Owner's sighting: it changes what he sees.** **2026-09-19 (afternoon) — CLOSED ON THE OWNER'S SIGHTING, on the live site.** He opened the site, tapped a trade, tapped **I recommend them too**, typed a name and words, and submitted. **The card showed nothing new — no words, no name, no tally** — and the thank-you message and the five-minute notice both appeared. **His words, verbatim: *"CHECK 1 PASS."*** That is precisely this row's done-when: the recommendation does not appear on the card until it has come back through the published feed, and both messages remain. The readability clause was measured before deployment — three recommendations at 320px on the largest text size and again at 200% zoom, **zero overflow at every size**, with the Call button at 827px whether the card carries none, one or three, because recommendations render below it. | 3 |
| 4.9 | done | A recommendation can never be shown against the wrong villager's name. Done when Published `K` and `L` are guaranteed to carry the same number of lines for every row on every write path, proven against adversarial sequences including a blank name followed by a named recommendation, a name containing a line break, and a first recommendation with no name at all. **A latent defect found in the Owner's live data 2026-09-19.** `K` (the words) and `L` (the names) pair **by line position**, and his real Published tab carries at least five rows with words in `K` and `L` entirely empty — the name is optional on the form, so this is normal and will recur. If any path ever appends a recommendation with a blank name by writing nothing to `L` rather than an empty line, **every later name on that row pairs with the wrong words** and a villager reads *"Recommended by"* against a neighbour who did not write it. Nothing would flag it. **Closes on the Owner's sighting: it changes what he sees.** **2026-09-19 (afternoon) — CLOSED ON THE OWNER'S SIGHTING, done in the exact order the defect required.** He recommended a tradesperson from the live site leaving the **name box EMPTY**, then recommended the **same person again WITH a name**. On reload the nameless recommendation reads **"a villager"** and the named one below it carries **the right name**. **That ordering is the whole test, and a general look at the cards could not have substituted for it:** under the old reader the blank entry would have been dropped and his typed name would have appeared against the FIRST set of words — the misattribution this row exists to prevent. He has now seen the case the defect actually lived in, in the arrangement that produced it. | 3 |
| 4.10 | done | A villager reads every recommendation on a card by scrolling, with nothing to tap first. Done when a card carrying several recommendations shows all of them in order with no expander, no "show more", no cap and no truncation, and a card with six is measured at the largest text size on a 320px phone and again at 200% zoom with nothing overflowing its box. **Owner's ruling, verbatim: *"it put in a button you had to click on to reveal the 'one more review' which is pointless. Just let it scroll!"*** **This row exists because of drift in Claude's own work, and is recorded as such rather than as a change of mind** — prompt 2718 said explicitly *"RAISE it rather than inventing a 'show more' control"*, and a control is what a villager now meets. See handover Layer 5. **Closes on the Owner's sighting: it changes what he sees.** **2026-09-19 (afternoon) — CLOSED ON THE OWNER'S SIGHTING.** He opened a card carrying more than two recommendations on the live site and confirmed **every one renders in order with nothing to tap**. **His word: *"4.10. pass"*.** That is this row's done-when: no expander, no "show more", no cap and no truncation. The readability clause was measured before deployment — **six** recommendations at the largest text size on a 320px phone and again at 200% zoom, **zero overflow at every size**, with the Call button at **827px whether the card carries none, one or six**, because recommendations render below it. | 2 |

| # | Human | Machine | Outcome | Approach | State | Due | Forecast | Owner | Depends on | Pinned start | Due start | Planned end | Actual start | Forecast end |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 4.1 | When a villager sees the electrician who rewired their kitchen last year already listed, they want to say so too. | Build the in-card recommendation panel | A second voice can be added to a listing without the owner touching the sheet | In-card form, no navigation away | done | 2026-09-18 | 2026-09-18 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 3.1 |  | 2026-09-18 | 2026-09-18 | 2026-09-18 | 2026-09-18 |
| 4.2 | When Gavin opens the Votes tab on a Sunday evening, the new recommendations are there and the listings untouched. | Deploy an append-only Apps Script endpoint | The open endpoint is bounded to junk rows at worst, never a changed listing | Bound Apps Script web app | done | 2026-09-21 | 2026-09-18 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 4.1 |  | 2026-09-18 | 2026-09-21 | 2026-09-18 | 2026-09-18 |
| 4.3 | When a villager taps Add and nothing visibly happens, they will tap it again and again. | Measure real cross-origin response behaviour | The optimistic thank-you rests on a measurement, not a guess | Live test from the Pages origin | done | 2026-09-22 | 2026-09-18 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 4.2 |  | 2026-09-21 | 2026-09-22 | 2026-09-18 | 2026-09-18 |
| 4.4 | When Gavin follows the deployment document, it must not tell him to do something that breaks the site. | Align comments, deploy steps and favicon with reality | Nobody is misled by a document or a comment that was true last week | Correct in source, measure on the served site | done | 2026-09-22 | 2026-09-18 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 4.3 |  | 2026-09-18 | 2026-09-22 | 2026-09-18 | 2026-09-18 |
| 4.5 | When a villager writes "Fixed gate" and the page refuses it, they assume they are at fault and give up. | Lower the note minimum to seven and tell the truth in the message | An honest short answer about a small job is accepted | Change the constant and the message, measure on the served page | done | 2026-09-22 | 2026-09-18 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 4.1 |  | 2026-09-18 | 2026-09-22 | 2026-09-18 | 2026-09-18 |
| 4.6 | When a villager writes a few honest words and the page thanks them, those words must actually arrive. | Align the endpoint's minimum with the page's | No recommendation is accepted on screen and dropped in transit | Change the constant, owner redeploys the same deployment | done | 2026-09-23 | 2026-09-19 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 4.5 |  | 2026-09-18 | 2026-09-23 | 2026-09-18 | 2026-09-19 |
| 4.7 | When a villager recommends a good electrician, the whole village sees it, not just that villager's own phone. | Repoint the endpoint at the Published tab, sharing the publisher's append | A recommendation placed on the site reaches the village list by itself | Same append function as the form route, bounded to K and L of one existing row | done | 2026-09-24 | 2026-09-19 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 4.6 |  | 2026-09-19 | 2026-09-24 | 2026-09-19 | 2026-09-19 |
| 4.8 | When a villager's recommendation quietly fails, they must not be left believing the village can see it. | Remove the optimistic render; keep the thank-you and the notice | No villager is shown words that never reached the village list | Render only from the published feed | done | 2026-09-26 | 2026-09-19 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 4.7 |  | 2026-09-19 | 2026-09-26 | 2026-09-19 | 2026-09-19 |
| 4.9 | When a neighbour reads who recommended someone, that name must be the person who actually wrote it. | Guarantee the words and names columns stay line-for-line aligned | A recommendation is never attributed to the wrong villager | Same-line invariant enforced on every write path | done | 2026-09-26 | 2026-09-19 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 4.7 |  | 2026-09-19 | 2026-09-26 | 2026-09-19 | 2026-09-19 |
| 4.10 | When an older villager wants to read what their neighbours said, they should not have to find a button first. | Remove the expander so every recommendation renders in order | Every recommendation is readable by scrolling alone | Render all, no cap and no truncation | done | 2026-09-26 | 2026-09-19 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 4.8 |  | 2026-09-19 | 2026-09-26 | 2026-09-19 | 2026-09-19 |
---

## Step 5 — Sheet clean-up

The responses tab flags its own duplicates and tells the owner what to do about each row.

| # | Status | Sub-task | Effort |
|---|---|---|---|
| 5.1 | done | The owner can review a new submission at a glance and act on it in one tap. ~~Done when each response row shows a plain-English verdict covering all four cases, tested with sample rows that trigger each, and carries a Publish / Add to T0xx / Reject dropdown.~~ **SUPERSEDED 2026-09-19 by the Owner's ruling D4b-6G7f-19092026 → Done when each response row shows a plain-English verdict, THREE of the four cases are sighted by the Owner in his own sheet, `NEW` is evidenced by the committed test harness rather than by sighting, and the tab carries a Publish / Add to T0xx / Reject dropdown.** **Why the original clause could never be met, and it is behaviour rather than a defect:** automatic publishing under D7a puts every form response onto Published within seconds, so a row holds the verdict `NEW` only in the interval between submission and publication. Established by the 2026-09-19 session against his live feed — every response on his tab had already published itself, so every one correctly reads `ALREADY ON SITE` and none can read `NEW`. Put to the Owner with three options; he ruled **D4b, correct the wording**. Declined: **D4a** leave the wording and close on three anyway, **D4c** build a way to pause the publisher and catch `NEW`. **Amended in place rather than rewritten**, because STD-00009 is silent on amending a row's acceptance wording (checked 2026-09-19: no rule covers it; 5.10.8 governs unfalsifiable rows and 5.7.4 duplicates, neither of which this is) — so this plan's own precedent is followed, set by row 5.2, which recorded a superseded clause rather than deleting it. The Sub-task cell is prose and is not machine-read, so a strikethrough here cannot mislead a parser. Normalisation and verdict definitions per solution design §7.2 and §7.3. **2026-09-18T15:06:40Z — FORMULAS WRITTEN AND LOGIC TESTED, not yet pasted. [PATH] `apps-script/SHEET-FORMULAS.md` carries paste-ready `phone_key` (K2), `name_key` (L2) and `verdict` (M2) array formulas against the real responses-tab columns A–J, plus the Publish / Add to T0xx / Reject dropdown setup for column N, and the ID-immutability rule. 12 logic tests pass in local simulation covering all four verdicts, both `CHECK THIS` triggers, the precedence of `CHECK THIS` over `ALREADY ON SITE`, and phone normalisation of the `+44` and `0` forms to one key. HAS NOT RUN IN GOOGLE SHEETS. Closes when the owner has pasted them and one sample row of each case shows the right verdict in the real sheet.** **2026-09-18T18:20Z — THE ABOVE WAS WRONG AND IS CORRECTED. The claim that those formulas were written "against the real responses-tab columns A–J" was never true: the responses tab is not published and cannot be read from outside the Google account, so the layout was ASSUMED, and the 12 tests confirmed self-consistency rather than correctness. The owner pasted his real header row and three real data rows on 2026-09-18T18:13Z (REPORTED — his sheet is the authority on its own shape). Real layout: `A` Timestamp, `B` Email Address, `C` Trade, `D` First name, `E` Last name, `F` Telephone, `G` Business, `H` Experience text, `I` Villager's name, `J` first free. The committed formulas assumed phone in `E`, name in `D`+`J`, experience text in `G` and business in `J` — **all four wrong; pasting them would have written nonsense into every row** without erroring visibly. REWRITTEN at `bd3f42a` and re-simulated. New target cells: **`J2` `phone_key`, `K2` `name_key`, `L2` `verdict`, `M` the dropdown** on Form responses, plus two helper columns **`I2`/`J2` on Published** — the cells have MOVED from the old `K`/`L`/`M`/`N`. Three further defects were found and fixed while rewriting, none caused by the layout: the verdict formula nested `ARRAYFORMULA` inside `COUNTIF`, which Sheets rejects outright (fixed via the Published helper columns; verified safe because `app.js` maps that tab by header NAME and ignores unknown columns); `0044` was not normalised, producing a different key for the same number; and `MATCH` was unguarded and would surface `#N/A` in the verdict text. 12 simulated cases now pass — the owner's three real rows plus nine constructed covering `NEW`, `ALREADY ON SITE`, `SAME NAME, DIFFERENT NUMBER`, `CHECK THIS` by short text, `CHECK THIS` by "not known", the precedence of `CHECK THIS` over `ALREADY ON SITE`, a blank row, and the `+44` form. **STILL NOT RUN IN GOOGLE SHEETS, and this row stays `new`** — its wording closes it only when the owner has pasted them and one sample row of each case shows the right verdict in the real sheet. One stated limitation rather than a hidden one: `name_key` matches a full name typed into the first-name box against a properly split Published row, and tolerates case, spacing and punctuation, but **cannot** match a name typed back to front. Recorded in the document rather than papered over. See Layer 3 for the root cause and Layer 5 for the trade-check recommendation deliberately not implemented.** **2026-09-18T18:30Z — THE `CHECK THIS` LENGTH TRIGGER IS NOW SEVEN, NOT FIFTEEN, at commit `81385b7`. Owner's ruling 2026-09-18 (~18:26Z), verbatim: *"I think we can change the check to 7 characters. As they can't really put in a sentence without more than that "Fixed Gate" is about as short as you can get."* Only the length clause changed: the two "not known" triggers (surname and business) and the precedence of `CHECK THIS` over `ALREADY ON SITE` are untouched. Re-simulated across the same twelve cases — **two verdicts change, both exactly as the owner predicted**: his real row 1 (*"top blokes"*, 10 characters) and real row 2 (*"Lovely people"*, 13) move from `CHECK THIS` to `NEW`, so honest short answers stop being flagged. The other ten are unchanged, including the precedence case (`C7`, text *"quick"* at 5 characters, still `CHECK THIS` despite its phone matching `T001`). **Boundary verified exactly: six characters flags, seven passes**, and the owner's own *"Fixed Gate"* passes. The same seven now applies on the website (row 4.5) so the page and the sheet agree. **STILL NOT RUN IN GOOGLE SHEETS, and this row stays `new`** — it closes only when the owner has pasted the formulas and seen one sample row of each verdict come out right in his own sheet.** **2026-09-19 — CLOSED under D4b on the Owner's sighting of three verdicts.** He pasted the corrected `L2` verdict formula — the `VLOOKUP` version at commit `81708cd` — into Form responses and reported, verbatim: ***"I have now pasted the correct code. And it seems to work."*** **The three sighted verdicts:** `CHECK THIS` (both test rows with the experience box blank), `SAME NAME, DIFFERENT NUMBER` (visible in his screenshot of the verdict column), and `ALREADY ON SITE` with per-row ids under the corrected formula. The branch selection was confirmed correct by that screenshot BEFORE the id fix, and the id fix changed only the lookup, so all three rest on his own screen. **`NEW` is evidenced by [PATH] `apps-script/test-verdict.js`**, which asserts it against the owner's real Published contents — exactly as D4b provides. **STATED RATHER THAN GLOSSED:** he did **not** quote the specific readings of rows 15 and 16 back, so this row does **not** rest on an individual confirmation that they read `T014` and `T015`. His report is that the formula works; the per-row ids are evidenced by the harness and by his screenshot showing distinct ids, not by him reading those two rows aloud. | 6 |
| 5.2 | done | A recommendation reaches the village list without the owner touching a spreadsheet. Done when a new form response appends one row to Published by itself, carrying the next id in sequence derived from the highest id already there, the name, business, phone and trade as plain values and never formulas, with `status` `active` for a clean submission and `hidden` for any of the three hard failures — no usable phone number, an email address or a web link in any field, or a phone already on Published. The script never edits, deletes or reorders an existing Published row and never touches Votes or the responses tab. A backfill the owner runs once from the menu brings existing responses across and is safe to run twice. Closes when a real form submission lands a row on Published and the owner confirms it. **2026-09-18T20:46Z — CLOSED on the owner's confirmation. He submitted the form from his phone and a row appeared on Published without him touching the spreadsheet. Reported verbatim: the repair said *"Repaired. Moved 14 people back up into the list. Removed 6 duplicate rows… Retired IDs (never reused): T013, T014, T015, T016, T017, T018. Put the missing 0 back on 12 telephone numbers. Corrected 5 trade names… Every ID is unchanged."* and of the new submission, that it published "but not column I or J" — the row landed, the helper columns were the separate defect. CHECKED CLAUSE BY CLAUSE against this row's own wording, and every clause is MEASURED on his real live feed rather than taken from his summary: a new response appended one row by itself (**T013 Ron Suttil**, absent before tonight); **the next id in sequence derived from the highest already there** (T012 → T013); name, business, phone and trade are **plain values, no formulas**; `status` `active` for the clean submission; `hidden` for the unusable phone (**T007**, twelve digits, correctly refused by the repair and off the site); and no existing row was edited, deleted or reordered — T001–T012 are all present and intact. The backfill's safety when run twice is stub-proven and was exercised by his repair without loss. **ONE CLAUSE IS SUPERSEDED RATHER THAN UNMET:** this row was written before D8a and names "a phone already on Published" as a third hard failure that should set `hidden`. D8a deliberately reversed that — a duplicate is now a SECOND RECOMMENDATION appended to the existing person, and the live feed shows exactly that working (T005 and T009 each carry a villager's words). The row's intent, that a duplicate must not quietly create a second listing, is satisfied by the better behaviour. **WHAT IS NOT YET PROVEN BY HIM, stated rather than glossed:** he has not reported seeing the recommendation text and recommender name render on a card. That was measured HERE against his real feed for the first time this session — nine of twelve cards show a villager's words, and Ron Suttil's card reads *"Very good engineers, especially good working with metal" — Recommended by Helen* — but it is this session's measurement, not his sighting.** | 6 |

| # | Human | Machine | Outcome | Approach | State | Due | Forecast | Owner | Depends on | Pinned start | Due start | Planned end | Actual start | Forecast end |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 5.1 | When Gavin cannot remember whether a submitted plumber is already listed, the sheet tells him. | Add normalising keys and the verdict column | Reviewing a submission becomes a glance instead of a search | Spreadsheet formulas on the responses tab | done | 2026-09-21 | 2026-09-19 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 2.1 |  | 2026-09-18 | 2026-09-21 | 2026-09-18 | 2026-09-19 |
| 5.2 | When a villager recommends a plumber on Sunday, the village can ring them without Gavin lifting a finger. | Publish form responses automatically, gating three hard failures | The directory keeps itself current with no admin | Account-only Apps Script on an on-form-submit trigger | done | 2026-09-23 | 2026-09-23 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 5.1 |  | 2026-09-18 | 2026-09-23 | 2026-09-18 | 2026-09-23 |
---

## Step 6 — Inheritance package

Someone handed the two logins can run the site, and can rebuild it from zero.

| # | Status | Sub-task | Effort |
|---|---|---|---|
| 6.1 | external | [owner: PLAN-DUNCHI-LAUNCH-2026-09-19] Someone handed only the two account logins can both run the site and rebuild it from nothing — verified by walking it, not asserted. Done when the root `README.md` explains in plain English what the site is, how to add, edit and remove a tradesperson, how to edit the page in the GitHub web editor with no build step, that ids are assigned once and never changed, and carries the three standing warnings (the five-minute republish lag, never click "Stop publishing", and deleting the Published tab breaks the feed while renaming it is safe); `docs/RESTORE-dunchi-trader.md` covers recreating the Form, Sheet, tabs, publishing, Apps Script deployment, repository and Pages in order with expected results; **and somebody has walked that runbook end to end confirming every step is achievable with those two logins alone.** Any step needing anything else is a defect to fix, not a footnote to add. **2026-09-18T15:06:40Z — BOTH DOCUMENTS WRITTEN, WALK NOT DONE. [PATH] `README.md` covers what the site is, adding/editing/removing a tradesperson, editing in the GitHub web editor with no build step, the ID-never-changes rule, and all three standing warnings. [PATH] `docs/RESTORE-dunchi-trader.md` rebuilds the Sheet, tabs, publishing, Form, repository, Pages, Apps Script and formulas in order, each step with its expected result and a final checklist. THE WALK IS THE OWNER'S AND IS NOT DONE — it needs the Form, Sheet, Apps Script and Pages under the two logins. Steps 3 and 7 of the runbook are additionally unverifiable while 2.1 and 4.2 are open: step 3's form-validation and email-off settings and step 7's deployment cannot be confirmed from outside the Google account. Closes on the end-to-end walk.** **2026-09-18T17:40Z — RUNBOOK RECONCILED TO THE DEPLOYED STATE AT `983525f`; THE WALK IS STILL OUTSTANDING AND THIS ROW STAYS `new`. The runbook was written before the Apps Script was deployed and before `VOTES_ENDPOINT` was set, so walking it as written would have wasted the walk and manufactured false defects. Corrected against the repo and the live site: step 2's example URL carried the superseded gid `1583719737` — the raw form-answers tab this project was already caught by once — now `1915382769` with a warning on how to spot the wrong tab; step 5's file list omitted `CLAUDE.md` and now lists every tracked file, with a note that the favicon is inline in `index.html` so a rebuild reproduces it by copying the repository rather than adding a file; step 6 now explains why its message differs from step 5's, which is what makes it proof rather than decoration; step 7 no longer tells the walker to paste into `VOTES_ENDPOINT` unqualified (it distinguishes the one-repository rebuild case from the two-repository live case) and no longer promises a result the page cannot show; step 3 now states which two settings cannot be confirmed from outside the Google account and gives an incognito test for the rest. **THE STEP-5 EXPECTED RESULT WAS MEASURED, NOT REASONED:** driven in a real browser against the live site with the feed intercepted, a header-only feed yields *"Nobody on the list just yet"*, a wrong-shaped feed yields *"The list is being updated"*, and an unreachable or 404 feed yields *"The list will not load at the moment"* — a control case with one active row rendered normally, validating the harness. The runbook previously claimed the pre-feed state shows *"The list is being updated"*; it names both plausible messages now, and says what would instead mean step 5 itself is wrong. A walk-through tick list was added at the end, one line per step naming what the walker should see, usable on a phone beside a laptop. Every clause of this row about the CONTENT of the two documents is now satisfied; **the clause that closes the row — "somebody has walked that runbook end to end confirming every step is achievable with those two logins alone" — is untouched by this session and remains the owner's.** No status change, and no effort cleared: this is documentation, not delivery.** **MOVED OUT OF THIS PLAN 2026-09-19 under the Owner's ruling D10-6G7f-19092026, and NOT descoped.** The work still matters and is owned by **`PLAN-DUNCHI-LAUNCH-2026-09-19`**; this row records the dependency and carries none of the weight (STD-00009 Rules 5.2.1, 5.2.2 and 5.2.4 [CORE] — `external` is the sanctioned token for work another Plan ID owns, `descoped` would have meant the work was dropped). Its effort is unchanged per Rule 5.2.5 [CORE] and is excluded from this plan's denominator by its status, never by re-weighting. The runbook was additionally found BROKEN on 2026-09-19 — step 4 names the live repository so the walk stops dead, steps 7 and 8 open the live spreadsheet by its hard-coded id, and there is no cleanup section. The successor's row covers repairing it as well as walking it. **Its text is preserved here in full so a reader following a citation to `6.1` still finds what it referred to.** | 9 |

| # | Human | Machine | Outcome | Approach | State | Due | Forecast | Owner | Depends on | Pinned start | Due start | Planned end | Actual start | Forecast end |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 6.1 | When Gavin hands the directory to whoever takes it on and walks away, they can run it without him. | Write README and runbook, then walk it | The inheritance requirement is proven rather than asserted | End-to-end verification walk | external | 2026-09-23 | 2026-09-23 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 4.2, 5.1 |  | 2026-09-18 | 2026-09-23 | 2026-09-18 | 2026-09-23 |
---

## Step 7 — Launch readiness

The site is ready for the link to go into the village WhatsApp group.

**Seeding the directory and posting the launch message are the owner's actions, not the
build's.** They are recorded as launch prerequisites in `docs/HANDOVER-dunchi-trader.md`
Layer 4. This step delivers only the readiness check against real seeded content and the
words he will post.

| # | Status | Sub-task | Effort |
|---|---|---|---|
| 7.1 | external | [owner: PLAN-DUNCHI-LAUNCH-2026-09-19] The seeded site has been proven end to end on the devices villagers will actually use. Done when, against the real seeded content rather than a test fixture, the whole journey works on a phone and on a computer — open the link, tap a trade, tap Call, add a recommendation — only populated trades appear, and the largest-text-size and 200% zoom checks still pass. **MOVED OUT OF THIS PLAN 2026-09-19 under the Owner's ruling D10-6G7f-19092026, and NOT descoped.** The work still matters and is owned by **`PLAN-DUNCHI-LAUNCH-2026-09-19`**; this row records the dependency and carries none of the weight (STD-00009 Rules 5.2.1, 5.2.2 and 5.2.4 [CORE] — `external` is the sanctioned token for work another Plan ID owns, `descoped` would have meant the work was dropped). Its effort is unchanged per Rule 5.2.5 [CORE] and is excluded from this plan's denominator by its status, never by re-weighting. It waits on the list being seeded, which is a villager-paced activity. **Its text is preserved here in full so a reader following a citation to `7.1` still finds what it referred to.** | 5 |
| 7.2 | external | [owner: PLAN-DUNCHI-LAUNCH-2026-09-19] Gavin has the words to post and knows the list is worth opening. Done when the launch message for the village WhatsApp group is drafted in plain language, saying what the site is and what it is not, and the live feed is confirmed to carry at least 12 tradespeople across at least 6 trades. **2026-09-18T17:26Z — FIRST HALF DELIVERED, ROW DELIBERATELY STAYS OPEN ON THE SECOND. The launch message is drafted in plain language in [PATH] `docs/PROCESS-seeding-and-launch-dunchi-trader.md` §4, reproduced verbatim as supplied by the owner's conversation and reproduced byte-exact into the document (verified programmatically, not by eye). It says what the site is — a list of recommendations from neighbours, with a large Call button and a text-size control — and what it is not: no stars, no scores, no complaints, no prices, nobody paid to be on it. It is published under a heading marking it a DRAFT AWAITING THE OWNER'S APPROVAL which states it must not be posted until step 7 has passed, with a pre-post checklist. THE SECOND HALF IS NOT MET AND CANNOT BE FAKED: the live feed was read this session at 17:25Z and carries 4 rows — `T001` Duckers Plumber, `T002` Test Sparky, `T003` Another Roofer, `T004` Hidden Person (`status` `hidden`) — i.e. 3 visible test people across 4 trades, against a requirement of at least 12 tradespeople across at least 6 trades. The seeding that closes this is the owner's action and is recorded in the handover Layer 4; the document above tells him exactly how to do it, including that ids start at `T005` and that the four test rows must be removed or hidden first. Status stays `new`.** **MOVED OUT OF THIS PLAN 2026-09-19 under the Owner's ruling D10-6G7f-19092026, and NOT descoped.** The work still matters and is owned by **`PLAN-DUNCHI-LAUNCH-2026-09-19`**; this row records the dependency and carries none of the weight (STD-00009 Rules 5.2.1, 5.2.2 and 5.2.4 [CORE] — `external` is the sanctioned token for work another Plan ID owns, `descoped` would have meant the work was dropped). Its effort is unchanged per Rule 5.2.5 [CORE] and is excluded from this plan's denominator by its status, never by re-weighting. It waits on the list being worth opening. **Its text is preserved here in full so a reader following a citation to `7.2` still finds what it referred to.** | 3 |

| # | Human | Machine | Outcome | Approach | State | Due | Forecast | Owner | Depends on | Pinned start | Due start | Planned end | Actual start | Forecast end |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 7.1 | When the first villager taps the link minutes after Gavin posts it, that first visit works. | Run the full journey against seeded content | Launch risk is retired before the link is shared, not after | End-to-end manual test on two devices | external | 2026-09-24 | 2026-09-24 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 6.1 |  | 2026-09-24 | 2026-09-24 |  | 2026-09-24 |
| 7.2 | When Gavin is about to paste the link into the village group, he needs a sentence to put with it. | Draft the launch message and confirm seeding | A thin directory cannot be launched by accident | Draft approved by the owner | external | 2026-09-25 | 2026-09-25 | 222b34c4-7d05-48f4-9d23-cfb47e96d9de | 7.1 |  | 2026-09-25 | 2026-09-25 |  | 2026-09-25 |
---

*End of build plan.*
