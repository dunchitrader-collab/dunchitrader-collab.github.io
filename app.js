/* Dunchideock Village Suppliers.

   Placeholder. The real behaviour — fetching the published Google Sheet CSV,
   grouping tradespeople by trade, search and autocomplete, the text-size
   control and the recommendation panel — arrives at build plan step 2 and
   step 3, ported from design/wireframe-dunchi-trader.html.

   There is no build step on this site. This file is served exactly as it is
   committed, so it must stay plain JavaScript that a browser runs directly. */

(function () {
  "use strict";
}());
